Put your shared test resources in this directory.

**IMPORTANT** - If you add new file types, then you may need to update file:
    *jper-sword-out/**MANIFEST.in***
so that the new files are deployed by `pip install .` (if you find they are absent when you run tests).

