# Robot Tests

NOTE that use of these Robot tests was discontinued several years ago after difficulties in keeping selenium configured & working; and when they did run they were tediously slow.  It is highly likely that the existing tests will fail as the following have happened since:
* GUI screens (HTML) changed
* CSS changed
* function names changed
* source-code filenames and locations changed.

(The tests were kept in case anyone is masochistic enough to get them running again).

